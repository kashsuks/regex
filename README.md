# Regecks

## Setup

Setup locally via the following command:

```bash
git clone https://github.com/kashsuks/regecks.git
```

Ensure you have [uv](https://docs.astral.sh/uv/) installed.

### Running Tests and Development Environment

Unit tests can be run via uv using:
```bash
uv run pytest
```
